.puppetmaster/
.codegraph/codegraph.db
.codegraph/codegraph.db-*
bench/results/*.md
bench/results/*.json
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
.env
.env.*
!.env.example
node_modules
node_modules/
package-lock.json
npm-debug.log*
*.vsix
*.log
.DS_Store
dist/
build/
*.egg-info/
coverage/
.coverage

# Separate research project — lives in its own repo (durable-state-vs-context).
# Never track or push it with Puppetmaster. Anchor at repo root so
# docs/research/ (product steal notes) remains trackable.
/research/

# Operator-only runbooks / personal notes — never ship in the product repo.
docs/PYPI_NAME_REQUEST.md

uv.lock
